import styled         from '@emotion/styled'

import { Column }     from '@ui/layout'

import { baseStyles } from './trigger-container.styles.js'

const TriggerContainer = styled(Column)(baseStyles)

export { TriggerContainer }
