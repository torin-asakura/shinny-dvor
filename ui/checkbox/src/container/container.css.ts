import { style } from '@vanilla-extract/css'

export const baseStyles = style({
  width: '100%',
  height: '100%',
  position: 'relative',
})

export const shapeStyles = style({
  width: '100%',
  height: '50px',
  display: 'flex',
  alignItems: 'center',
})
